#! octave-interpreter-name -qf
# predict Tic-Tac-Toe
addpath('../NeuralNetwork')
load Theta1.mat;
load Theta2.mat;
load Theta3.mat;
load pData.mat;
[dummy, p] = max(predict(Theta1, Theta2, Theta3, pData), [], 1);
printf (num2str(p));
