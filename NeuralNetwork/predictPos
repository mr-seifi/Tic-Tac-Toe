#! octave-interpreter-name -qf
# predict Tic-Tac-Toe
cd ../NeuralNetwork;
load Theta1.mat;
load Theta2.mat;
load Theta3.mat;
load realtimeData.mat;
[dummy, p] = max(predict(Theta1, Theta2, Theta3, realtimeData), [], 1);
printf (num2str(p));
