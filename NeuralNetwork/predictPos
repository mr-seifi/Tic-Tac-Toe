#! octave-interpreter-name -qf
# predict Tic-Tac-Toe
load Theta1.mat;
load Theta2.mat;
load Theta3.mat;
load pData.mat;
[dummy, p] = max(predict(Theta1, Theta2, Theta3, U), [], 1);
printf (num2str(p));
